<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.free.decision.server.mapper.ZhiMiMapper">

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    insert into decision_zhimi_record (request_id,user_id, result,create_time) values (#{requestId},#{userId},#{result},now())
  </insert>

</mapper>